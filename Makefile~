all:
	mkdir -p build
	cd build && cmake -DCMAKE_TOOLCHAIN_FILE=../toolchain-linux-i486-mingw32-toolchain.cmake ../ && make

install: all
	cd build; make install "DESTDIR=/home/link2012/Área de trabalho/SteamApps/Grand Theft Auto San Andreas/"

clean:
	rm -rf build
	rm -rf bin



